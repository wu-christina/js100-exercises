let a = 1;

function myFunction() {
  console.log(a);
}

myFunction();

/**
 * It will log 1.
 * variable a is declared on a global level so it can be accessed by the
 * function in line 4 when invoked.
 */